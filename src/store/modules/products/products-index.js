import featured from 'vuex-store/modules/products/featured';

// initial state
const state = () => ({});

const getters = {};

const actions = {};

const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
  modules: { featured },
};
